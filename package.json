{
  "name": "webgame01",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "core:test": "pnpm --filter @sengoku/core test",
    "core:headless": "pnpm --filter @sengoku/core headless"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
